import styles from './BaseInfo.module.scss';

type BaseInfoProps = {

}

const BaseInfo = (props: BaseInfoProps) => {
    return ()
}

export default BaseInfo;


// article base info on top - category and city
// article base info on link to separate articles - logo and bias label